= content_for :head, auto_discovery_link_tag(:atom, discussions_url(trending_ignored: 'true', format: :atom))
= content_for :head, auto_discovery_link_tag(:rss, discussions_url(trending_ignored: 'true', format: :rss))

= render partial: 'nav'

.card.border-top-0{style: 'padding-top: 0'}
  .card-block
    .row
      .col-md-12

        %br
        %br

        .jumbotron
          %h4 Up to the top 25 posts that have been ignored (muted) on trending ...

        %small
          %ul
            - @discussions.group_by{ |d| group_pattern(d) }.sort_by{ |d| d.first }.reverse.each do |group|
              - key = group.first
              - from_size = key.first
              - from = key.second
              - amount = key.last
              - discussion = group.last.last
              %li
                #{pluralize from_size, 'ignore'} by
                %strong= from.to_sentence.html_safe
                - if group.last.size == 1
                  \-
                  = link_to discussion[:title], "#{site_prefix}#{discussion[:url]}"
                  by
                  = link_to discussion[:author], "#{site_prefix}/@#{discussion[:author]}"
                  %font.reputation= discussion[:author_reputation]
                - else
                  %ul
                    - group.last.each do |discussion|
                      %li
                        = link_to discussion[:title], "#{site_prefix}#{discussion[:url]}"
                        by
                        = link_to discussion[:author], "#{site_prefix}/@#{discussion[:author]}"
                        %font.reputation= discussion[:author_reputation]
